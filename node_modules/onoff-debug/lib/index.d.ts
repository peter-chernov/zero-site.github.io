import { BinaryValue, Edge, Options } from 'onoff';
declare class Gpio {
    static states: number[];
    private gpio;
    private original;
    constructor(gpio: number, direction: any, edge?: Edge, options?: Options);
    static init(): void;
    static info(): void;
    readSync(): number;
    writeSync(value: BinaryValue): void;
}
export { Gpio };
